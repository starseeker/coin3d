# =============================================================================
# tests/tools/CMakeLists.txt
#
# Unit tests for the SbModernUtils (tools/ subsystem) and other
# tool/utility modules.
# =============================================================================

# test_tools_suite.cpp was migrated to obol_unit_tests.  Keep this directory
# while other tool tests are added, but do not create a second hand-written
# runner for the same coverage.
